# Предрелизное TODO

- Проверь все страницы сайта и убедись, что они отдают нормальные и структурированные SEO данные: заголовок, описание, JSON стурктурированные и расписанные хлебные крошки и так далее.

- Система бекапов, которая просто целиком копирует папку, можно с поставкой какого-то скрипта, который и в windows и linux запускает авто-задачу, которая
  раз неделю через какой-нибудь API рут или еще как-нибудь выгружает все содержимое content. Распланируй так, чтобы оно стабильно работало даже при большом количестве файлов.
  Еще учти, что ведь есть и планировочные процессы по очистке например так что надо это тоже учитывать и сделать так, чтобы не получить поломанное состояние.

  Бекапы пока только в таком формате и планируются - кто хочет поднимает на своей машине этот автоскрипт и он туда же по папочкам и копирует содержимое.
  Причем можно либо настроить авто-бекап при активной машине раз в неделю, либо же запустить вручную бекап в любое время, после чего неделя начнет отсчитываться от этого момента.
  Хранить не более трех бекапов. Новый заменяет самый старый. Ручной бекап не идет в счет и именуется отдельно, чтобы различить можно было.
  Системы восстановления из бекапа сделать, достаточно описать инструкцией, что надо выключить сервер, заменить content на нужную и снова запустить. Этого ведь достаточно?

- Проверить логику использования кнопки back в браузерах, потому что при работе с многократно вложенными модалками она похоже нарушает последовательность действий.
  Например в модалке редактирования контента и добавления блока медиа я снаачала выбрал Переиспользовать, а потом по back решил выбрать "Загрузить". Загружаю, создаю вариант, нажимаю "Использовать" и вижу просто страницу: без каких-либо модалок - блок не вставился и самой модалки с контентом описания проекта нет, она почему-то то же исчезла.

# v0.0.2

- Автогенерация OG Image как на cult.gwynerva.net с favicon, иконкой первого медиа и данными.

- Адаптивные размеры изображений и `srcset`. Сейчас у каждого медиа ровно один
  превью-вариант с длинной стороной 720px, поэтому телефон качает полноразмерный
  hero. Добавлять можно без миграции медиатеки: производная — это просто новая
  строка `assets`, существующие файлы трогать не нужно.

- Кеширование публичных ассетов. Сейчас `public, max-age=0, must-revalidate`,
  то есть каждая картинка — условный запрос с round-trip. URL стабилен на время
  жизни ассета, а байты неизменны, так что здесь можно заметно прибавить; мешает
  только то, что доступ может смениться с публичного на приватный, поэтому
  `immutable` не подходит.
