## На завтра/сегодня

- Сломался drag and drop файлов проекта. Проверь, работает ли в editor-js для галереи
- Проанализировать drag and drop механики на сайте в разных местах (файлы проекта, editor-js блоки, ассеты в editor-js блоке, теги, связанные проекты, внешние ссылки)
  и создать единый универсальный механизм для всего этого. А лучше может быть даже задействовать для всего этого единую популярную и современную библиотеку, чтобы сложными расчетами и вычислениями не заниматься.
  Проверить на корретность работы решения браузерным навыком для всех drag-drop механизмов на сайте причем в десктопном формате и с эмулюцией тачей.

- Проверить, используются ли SVG изображения из assets/fallback. Если нет, то удалить fallback папку целиком.

- Отдельный блок настроек на странице редактирования "Кнопка действия проекта".
  Есть общий Toggle контроллер, который указывает "Показывать кнопку действия?". Если выключена, все остальное не показывать.
  Внутри из базовых настроек можно установить текст на кнопке (макс 30 символов), ее акцентный цвет, ассет фона кнопки (сделать отдельный пресет под это, использовать существующую инфраструктуру модалок файлов), а так же ассет иконки (то же самое - свой пресет, инфраструктура модалок файлов). Наконец, можно toggle переключателем указать, что эта кнопка приватная (по аналогии с приватными файлами и внешними ссылками).
  Далее идет переключатель между целью "Действия": Файл или Внешняя ссылка.
  В случае файла допускается загрузить любой файл используя инфраструктуру модалок.
  В случае ссылки нужно указать отдельным текстовым полем, куда она ведет.
  Если был загружен файл, то он должен быть связан с проектом и при последующей смене типа на ссылку или вообще удаления кнопки дейсвтия должен почтиняться правилам очистки неиспользуемых ассетов.
  Если в качестве иконки кнопки действия не было ничего выбрано, то показывать иконку внешней ссылки если тип кнопки ссылка и иконку файла если это файл.
  Сделать превью область, где эту самую кнопку показывать со всеми примененными настройками.
  Клик по ней открывает ссылку на сайт/файл в новой вкладке.
  Кнопка должна быть универсальным по всему сайту компонентом.
  Для фона кнопки если загружен ассет должны быть раздельные переключатели, которые повзоляют растянуть/повторять этот фон по вертикали/горизонтали.

  Код должен быть написан профессионально, чисто, без багов и возможных конфликтов, без захламления системы и с переиспользованием уже объявленных ранее универсальных компонентов.
  Интерфейс этого блока с найстроками должен быть красивым и лаконичным, в духе других форм в админ панели.

  Проверить весь функционал браузерным скиллом в декстоп и мобильном формате.

## Блок "Взгляд в прошлое"

На главной странице над или под "Важными проектами" выводить блок "Взгляд в прошлое",
который показывает, над какими проектами в этот самый день (неделю/месяц) человек работал год назад, два года назад и так далее.
Прикольный блок, который обновляется каждый день и все время что-то новое дает.
Плюс причины поностальгировать дополнительные.
