# v0.0.2

- Полноценная поддержка запуска сайта из подпапки (`app.baseURL`). Сейчас в
  `thei.config.json` есть `siteUrl`, но он принимает только origin: адрес с
  путём отклоняется настройками, потому что одного origin для подпапки мало.
  Что нужно: выставлять `app.baseURL` на сборке из `thei.config.json` в
  `update/instance/nuxt.config.tmpl.ts`; один base-aware хелпер поверх
  билдеров путей в `shared/*-url.ts` и поверх всех клиентских
  `$fetch('/api/...')` (~92 места), плюс `/favicon.svg`, `/icons.svg` и
  `window.location.href = '/admin/'` в `sign-in.vue` и `InstallForm.vue`.
  Nitro монтирует роуты по `app.baseURL`, а `TheiLink` резолвит через router
  base, так что они уже готовы — но h3 отдаёт middleware путь без префикса,
  когда route у layer длиннее `/`, и это надо учесть в `0.global.ts`.
  Отдельно: `robots.txt` из подпапки краулеры игнорируют, он обязан лежать в
  корне домена.
  Проанализируй, как Nuxt и Nitro работают с baseURL. Где-то они сами его добавляют при использовании NuxtLink (вроде бы),
  а где-то при составлении ссылки надо вручную его подставлять. Это скользкая тема довольно, возможно свой composable придется написать поверх.
  Четко этот момент обдумай.

- В настройках сайта отдельная возможность указать GTag, верефикационный код гугла и ID Яндекс Метрики, Яндекс мастера и чтобы все это тоже вставлялось в код head страницы наверное.
  Возможность короче подключить аналитику. Проанализируй их документацию, чтобы точнее понимать, как правильно и наименее инвазивно минимально внедрить это все, без обширных интеграций.

- Возможность создавать "ссылки для входа" одноразовые действующие час скажем самоотчищающиеся. Чтобы можно было на компе сгенерить отправить самому себе на телефон и нажав на нее автоматом войти в админку создав сессию.
  Без необходимости вводить пароль и прочее.
  Стоит ли вообще такую фишку реализовывать?

- Менять SEO title и description и заголовок вкладки добавляя в кавычках введенный запрос на странице поиска. Подумать, улучшит ли это SEO оптимизацию для поисковых движков.
  Ведь тогда самые популярные поисковые запросы смогут тоже как отдельные поисковые результаты в Google или Yandex появляться. Например "Baldur's Gate 3" - Поиск - Петра Радько и всякое подобное.
  И SEO описание тоже какое-нибудь понятное по типу "Все проекты и события, которые связаны с ЗАПРОС В КАВЫЧКАХ" или как-то похожим образом.

- Подумать как-то может дать возможность создания особого вида ссылок, которые позволяют временно просматривать выбранный приватный проект со всеми его приватными внутренними данными.
  Это можно оформить как админская опция может на странице редактирования проекта/события в отдельном блоке с вертикальным списком доступных токенов/ссылок и с указанием, через сколько они иссякут, с возможностью на месте прибить ссылку, продлить или создать новую. Вроде интересная идея, да? Поделиться временно с кем-то доступом без страха, если человек забудет ее на своей стороне удалить и она через какое-то время "утечет" в сеть, но работать то уже не будет! Из опций 30 минут, 1 час, 6 часов, сутки наверное.

- Автогенерация OG Image по самому популярному прямоугольному стандарту (с указанием размеров и прочего) примерно на cult.gwynerva.net (https://cult.gwynerva.net/og/default.png, https://cult.gwynerva.net/og/asset/90nwshUEL.png).
  В составе OG Image всегда должна быть favicon сайта с его названием, а дальше уже происходит разделение на два типа: служебные страницы и страницы с возможностью получить медиа для "карточки".
  Большая часть сущностей имеет возможность получить то или иное медиа для "карточки" (иконки тегов, проектов, страниц, первое медиа событий, fallback иконки с акцентными цветами на осве названия и так далее).
  Вот это медиа и должно использоваться в качестве постера слева растянутое на всю высоту и с подложкой в виде акцентного цвета. Далее уже скошенный угол и справа уже информация с favicon и названием сайта сверху,
  Крупным жирным названием по центру вертикальному и может еще снизу поясняющими данными (что за сущность проект/событие/этап проекта/раздел проекта/тег/страница и т.д.)
  Для главной публичной страницы по центру крупно favicon с именем пользователя и слоганом сбоку.
  Для других служебных страниц какие-то варианты с иконкой страницы обычной но без постера, чтобы отличить можно было контентную страницу от служебной публичной.

  Админ-страницы и вся админ-часть OG изображений не имеют вообще.

  Во всех случаях текст не должен выходить за границы изображения и должен адаптивно масштабироваться (без масштаба пару строчек, потом масштабировать вниз).
  После какой-то нижней планки обрезать в троеточие.

  Вот здесь кое-как не очень опрятно был реализован этот механизм, а вообще надо поискать проанализровать, как наиболее правильно такие вещи генерировать:
  https://github.com/Gwynerva/cult

  Предусмотреть какой-то механизм, который будет указывать на возможно обновленную версию, чтобы показывалась актуальная OG картинка. Может просто кеш небольшой по длительности или какой-то параметр в URL добавлять. Подумай как лучше будет.
  Опять же это не постоянные ассеты, их бекапить не надо, они должны в generated-media или еще как-то лежать вне системы бекапов.

- Реализовать блок "активности за год" с 365 квадратиками как в профилях GitHub разбитые на сетку по горизонтали месяцы по вертикали дни недели, которые могут иметь оттенок разной насыщенности в зависимости от того, сколько хронологических событий произошло в этот день.
  При наведении на квадратик показывает, сколько "событий" и какого типа произошло. При нажатии на квадратик под этими квадратиками в подвале блока выводится выбранная дата и карточки событий в этот день.
  Так же пока не выбрана конкретная дата (или под карточками событий, если дата уже выбрана) привести сводку из горизонтального списка "Проекты в этот год" из иконок проектов (считать по этапам проектов, а не по дате создания/обновления страницы проекта)
  Так же где-нибудь сбоку сделать легкий и удобный переключатель годов может в формате бокового вертикального списка, который в горизонтальный превращается при мобильной разметке, где можно кликать по предыдущим годам и тогда
  будут отображаться другие данные в квадратиках уже за выбранный год.

- Проанализируй не заставляет ли текущая система со скрытием изображений и видео, которые выходят за область просмотра, при обратном их появлении снова их загружать.
  Возможно, это все я слишком сам себя передумал и проще вообще уже загруженное никак не трогать и не скрывать, только видеоролики на паузы ставить.
  Так получиться наверное избежать этой анимации плавного появления, которая наверное по семантике и не очень здорово выглядит (ведь я уже "загрузил", почему оно снова "плавно появляется"?)
  Если действительно никаких потерь почти нет и браузеры сами умеют работать с большим количеством картинок "вне" viewport, то тогда оставить всю эту красивую анимацию проявления любых медиа только при первом показе и более не загружать систему лишним большим количеством обсерверов.

- Кеширование публичных ассетов. Сейчас `public, max-age=0, must-revalidate`,
  то есть каждая картинка — условный запрос с round-trip. URL стабилен на время
  жизни ассета, а байты неизменны, так что здесь можно заметно прибавить; мешает
  только то, что доступ может смениться с публичного на приватный, поэтому
  `immutable` не подходит.

- При указании favicon возможно, надо больше действий совершать: создавать png и ico версии (ico чтобы резолвилась по root пути наверное), плюс touch иконки генерить с затемненным (осветленным?) акцентнцым цветом, взятым из той же иконки?
  Короче целый пайплайн похоже надо подцепить к загрузке favicon. Проанализировать современное положение дел по этой сфере, вычленить только реально необходимые шаги и их и реализовывать.
  В админ странице наверное лучше плитку с favicon вынести на свою отдельную строчку выше плиток аватара и баннера и сбоку от нее сделать предпросмотр favicon в разных типовых размерах для favicon, а также непосредственно как-бы во вкладках браузера в светлой и темной теме,
  чтобы максимально наглядно было. Заодно favicon на отдельной странице разгрузит и без того очень тесную строку с аватаркой и баннером.
  Если там требуется больше настроек или одной строчки может не хватить, то тогда вообще все что связано с favicon и настройками с ним можно выделить в отдельный блок на странице со своим заголовком и поясняющей подписью, что эта иконка выводится во вкладках браузера и в поисковых системах.
  Сами дополнительные медиа-варианты в популярных форматах создавать наверное надо не как отдельные ассеты (они все равно нигде не будут использоваться кроме цели favicon), а где-нибудь в generated media. Короче надо это все аккуратно продумать, действительно ли так правильно.
  Учти дополнительно, что для favicon (как и для всех остальных мест сайта) могут грузить SVG файлы и выбирать "использовать напрямую", так что любые твои задействованные инструменнты должны и это учитывать корректно.

- Добавить возможность менять уже добавленные ранее статусы (текст и иконку) через открытие такой же модалки. Только там уже должно быть Сохранить, а не Добавить.
  Сделать аккуратно, без дубилкатов кода, с грамотным переиспользованием. Иконку чтобы можно было удалить через открытую модалку (вроде уже есть такая переиспользуемая опция).

- В блоках публичной гланой странице сделать угловую акцентую подстветку чуть менее агрессивной (поменьше и попрозрачнее)

- На главной странице админки верхнюю панель состояния визуально прокачать, сейчас она неэффективно использует место:
  - Индикатор свободного места можно сделать в виде небольшой круговой диаграммы размером с небольшую иконку, а не длиннющей полоски.
  - Можно добавить предупреждение желтое, если можно обновиться на новую версию.
  - Индикаторы, что не было копий или когда была последняя тоже можно как-то компактнее.
  - В идеале все данные вписать в одну строку компактно с дополнительнми title-popup подскасками при наведении, которые расшифровывают и дают пояснение
  - А еще сам этот блок состояния должен иметь такой же вертикальный отступ от идущих далее блоков, как и gap между ними, чтобы единообразно все выглядело.

- На главной странице админки в блоках и в целом по админке надо пробежаться и нормалоизовать описания и подсказки.
  Чтобы они примерно похожие по длине были и при этом несли дополнительную семантическую информацию.
  Плохой пример "Страницы: Все отдельные страницы этого сайта". Спасибо, гений, я в курсе. Лучше в описании было бы сказать, что на например на страницах можно хранить статический контент с красивым адресом или что-то вроде того.
  Короче провести ревизию с учетом того, что уже есть семанита использования этого сайта и движка.

- Подумать, нужно ди делать API уровень специально для ИИ агентов.
  Я в этой теме плаваю и не знаю, действительно ли это нужно, но в теории наверное было бы удобно, если бы ИИ могли иметь свои API забросы, которые выдают информацию сразу в текстовом формате с нужными ссылками на медиресурсы и ссылками на другие связанные материалы. И это описать в AGENTS.
  Реально проанализируй, насколько это нужная штука с учетом того, что вполне возможно будут в скором будущем запросы по типу "поправь грамматические ошибки в текстах" или "вот тебе разбросанные данные, составь из них самостоятельно новый проект".
  Нужны ли для этого отдельные API руты по выводу данных и быстрому вводу или нет? Естественно с соблюдением всех настроек приватности по проектам, событиям, приватным файлам, ссылкам и секциям.
  Возможно может быть как-то можно будет давать админ-токен-доступ нейросетям (переиспользовать нашу систему с "вход-ссылками")? Короче подумай и профессионально проанализируй "машинный интерфес" взаимодействия с данными движка.
  Может сюда же стоит добавить возможность какого-то минимально короткой документации по доступным editor-js блокам с инструкцией обвнолять эту докуменатацию при внесении любых изменений, но при этом не раздвувать ее бессмысленной информацией (все только по факту как есть).

- На странице событий в боковой панели связанных проектов не отображаются указанные причины связи, только описание проекта.
  Описание проекта должно использоваться только как fallback, если пояснения для связи нет!

После завершения работы проверь все в браузере и сделай ключевые снимки измененных моментов.

---

Второй пак правок для версии v0.0.2 (с отдельным планом).
Если потребуется менять форматы и вид URL спокойно меняй и не обеспечивай обратную совместимость старых ссылок.
Если нужно добавлять новые тестовые данные, добавляй их разнообразно в любом количестве и оставь в playground проверки, чтобя мог сам отдельно проверить.
Если нужно входить и выходить из админ панели, то используй имеющийся механизм fallback password простенький, в playground ничего конфиденциальноо нет.

- Везде, где самостоятельно указываешь дату (этапы проектов, даты событий, может где-то еще пропустил, но не день рождения) и где это имеет смысл ввести дополнительный FieldSelectedBar (или как там правильно такие штуки должны называться в UI)
  такой бар с кружочком-курсором разбитый на секции, который можно перемещать по этим дискретным позициям. Название компонента сам продумай, как и его стиль, но чтобы красиво, лаконично и в стилистике сайта.
  Суть этого бара в том, что у него есть 4 позиции. Первая по умолчанию - точная дата, остальные три добавляют степень неуверенности: не факт, что этот день , не факт что эти день и месяц, не факт, что день, месяц и год (точность до дней, до месяцев, до годов)
  При активации любой степени неуверенности добавлять текстовое поле FieldInput, в котором можно опционально дать пояснение насчет неуверенности.
  В чипе временного промежутка этого (в том числе в админке), а так же в публичной части где выводится информация об этой дате или промежутки для любой степени неуверенности использовать иконку approx икривленного волной знака "равенства" перед самой датой/промежутком и саму иконку вместе с датой окрашивать в соответствующий цвет (обычный, желтый, красный) и с title-popup подсказкой при наведении, в которой описан уровень неуверенности + конкретное пояснение от админа если есть (не забывать стакать это с расшифровкой относительной даты, если имеется).
  Пожалуйста, обязательно проверь, что этот бар неуверенности с пояснением не ломают попап выбора даты в desktop и в mobile, не появляется внезапных непонятных отступов, переполнений, размазываний плиток календаря и так далее.
  Ранее с этим элементом было ну очень много проблем. Если не получается напрямую внедрить, возможно стоит как-то иначе этот попап вообще переиграть, например часть сделать от библиотеки, а часть своим интерфейсом.

- На пуличной части вообще ВСЕ тексты, которые не являются частью UI, а берутся из введенных данных, выводить через нормализатор разделенный на языки как в системе Erudit: с заменой кавычек, подстановкой неразрывных пробелов перед предлогами и прочими зависящими от языка нормализаторскими фишками (например корректные символы апострофов в английском тексте). Все это должно быть гибко, расширяемо и не должно изменять сами данные, это только вывод!

  https://github.com/erudit-js/erudit/blob/main/packages/erudit/app/composables/formatText.ts

  Во всех FieldInput, FieldTextArea и редакторе editor-js подготовить расширяемую и гибкую систему умных замен. Начать с умной замены двойного дефиса "--" на тире "—" только тогда, когда это имеет смысл (например не когда безпробельно эти дефисы окружены) и три точки подряд "..." в символ троеточия. Причем сразу после замены если был нажат backspace в любой раскладке или его аналог (например на мобильниках), то не удалять символ, а сначала вернуть исходно введенные. То есть дать возможность отменить действие умной замены.
  Работать автозамена должна везде, даже в подписях к картинках и везде-везде-везде, где админ заносит любые данные в админке.

- Пробегись по полям на страницах админки и включи проверку грамматики в местах, где это нужно (в названиях событий, сутях событий и проектов, описаниях и так далее)

- В обработчике Ctrl + S так же не вызывай стандартное окно сохранения в стиуациях, когда сохранение предполагается возможным, просто не может быть выполнено из-за того, что например не все поля заполнены.
  Просто ничего не делай в этот момент. А то нарушается предсказуемость поведения - по привычке иногда жмешь комбинацию, даже если условия для сохранения не выполнены, а оно тебе открывает окно сохранения страницы нативное браузерное.

- У проектов и событий добавить отдельный блок

- Иконки файлов, иконки ссылок, иконки проектах в списках ссылок, файлов и связанных проектов в боковых панелях на страницах проектов, страниц, событий и т.д. выводить по верхней границе данных, а не центрировать по центру от заголовка + описания.
  У иконок внешних ссылок и внутренних ссылок на внутренние сущности выводить без подложки фоновой (но сохранить скошенные границы). Подложку у файлов не трогать.

- Добавить на админ-страницы редактирования событий, проектов и страниц еще один блок "Примечания" с семантически понятным описанием. Этот блок в самом низу находится.
  Внутри него FieldContentEditor поле заметки, которое имеет свою уникольную иконку и отображется в самом низу публичных страниц проектов, событий, страниц и только для администратора (не забыть быструю ссылку в Содержание добавить в боковой панели). Исходно тображаются только первые 2 блока, для раскрытия остальных нужно нажать на кнопку-текстовую "Показать все".
  А перед полем заметки создать поле напоминания в виде FieldTextArea. Если там есть какое-то содержимое, то в публичной части в любых карточках сверху сбоку справа от даты выводить еще и значок warning желтый с подсказкой, что у этой сущности есть активное напоминание. На карточках публичного Поиска тоже выводить этот же значок желтый.
  На самой странице сущности под названием и описанием выводить желтый стандартный бокс с иконкой warning и текстом этого напоминания.
  Не-админам ни заметки, ни напоминания не видны.
  А еще показывать warning иконку и в админских списках этих сущностей рядом с иконкой доступа только левее всего (левее доступа, витрины или резюме).

- На публичной странице выводить дату рождения с полным месяцем текстовым

- В editor-js контенте дать возможность при создании любых ссылок: внутренних или внешних отдельным полем указать "Пояснение" к ссылке.
  Оно будет отображаться вместо заголовка ссылки в боковой панели на публичных страницах (однако при склеивании одинаковых ссылок приоритет все равно отдаем названию если ссылка вручную была добавлена).
  Только аккуратно этот момент оттестируй в desktop и в mobile и в разных местах редактора (ближе к левому и к правому краям модалки), потому что эти всплывающие поповеры очень капризные и странно себя иногда ведут (может заодно посмотри, если там с ними проблемы или нет)
  Причем сделать так, чтобы пояснение можно было редактировать и в перевызываемом попапе уже после создания.

- На странице Поиск при наведении на каротчку найденную граница должна совпадать с ацентным цветом, а так же должна появляться тень акцентная как у карточек "Жизни" при наведении, только меньше, с учетом уменьшенного отступа у результатов поиска.
  Можно так же маленький эффект плавного подъема добавить при наведении, но аккуратно, главное не переборщить.

- У вертикальной полосы Жизни слева, а так же у этой же полосы на странице проекта среди этапов нужно добавить разные виды "промежутков" между днями с содержими каким-то.
  Предлагаю три вида промежутков: пунктир dash для разрывов в пределах одного месяца, пунктир dotted для событий разрывов от месяца до трех и специальный стиль "полного разрыва соединения" с неровными краями для любых разывов дольше трех месяцев.
  Подписи вида "через ..." при этом сохранить.

- При сохранении editor-js контента внутри модалки сохранять и всю сущность (проекта, события, страницы, Обо мне и т.д.) целиком в том случае, если изменение было внесено только в этот контент и не сохранять автоматом сущность, если что-либо другое было изменено.
  Пример: пользователь зашел в проект, открыл какой-то этап, внутри него контент этапа, там что-то подправил и нажал Ctrl + S или кликнул на кнопку "Сохранить". Так как он больше ничего не менял, то сохранить нужно и контент редактора, и этап, и проект.
  А вот если например были внесены другие изменения в данные этапа или это новый этап/раздел, для всего этого требовать ручное сохранение уже с закрытыми модалками на уровне страницы редактирования сущности.
  Это нужно чтобы можно было быстрые правки вносить и проверять на публичной части без постоянного закрытия модалки с редактором только лишь ради того, чтобы сохранить еще и сущность, а потом снова открывать редактор.
  Убедиться, что сравнивается именно с текущим состоянием формы, а не ее состоянием на момент загрузки. То есть человек может открыть страницу редактирования, что-то поменять, сохранить, открыть редактор и после изменения в нем сохранение в редакторе должно сохранить всю страницу целиком, так как предыдущие изменения он уже сохранил, хоть и не перезагружал страницу.

- В мобильном режиме в панели Сводка при нажатии на быструю ссылку из Содержания не обязательно ждать закрытия плавного сводки и только потом прыгать к нужному месту.
  Можно сразу прыгнуть, не выстраивая сложного тяжелого поведения с ожиданием, а когда Сводка естественным образом скроется плавно, человек уже видит, что страница на нужном месте проскроллена.
  Если только для такого скролла строго после закрытия Сводки у тебя не было каких-то более веских причин делать именно так.

- На страницах, для которых установлена какая-то кастомная иконка (не fallback) не показывать подложку для этой иконки. Только само изображение иконки.
  Скругление оставить - вдруг кто-то иконку сделает полноценным прямоугольным изображением.
  К таким страницам относятся страницы тегов, "страниц", может еще чего-то.

- Поменять отображение иконок в Сводке для событий, проектов, страниц и прочего.
  Вообще не показывать количество заголовков ни в одной сводке. Вместо этого на странице проектов как прежде показывать количество этапов, разделок, файлов, ссылок.
  А на страницах событий и "страниц" показывать количество и количество файлов, которые находятся в содержании события или страницы. Если 0, то этот счетчик не показывать.

- Как-то сделать так, чтобы универсальные кнопки Смотреть все сбоку от подзаголовках на разных страницах (на главной публичной, на странице проекта и т.д.) занимали меньше места в мобильном формате.
  Желательно, сильно меньше места. Плюс у некоторых из них счетчики есть, а у некоторых нет, надо тоже это свести к единому формату, который в мобильном формате при узкой ширине занимает минимум места, желательно без текста или коротко "Все".
  Подумай, как это толково по уму сделать красиво.

- В SEO заголовках и заголовках вкладок на всех страницах используй обычный дефис вместо длинного тире. Так компактнее выглядит и легче читается.
  В том числе и на главной странице если есть псевдоним замени | на -

- Определенные конфигурациям поиска тоже дать свой уникальный SEO загловок, описние и название вкладки.
  В частности: проекты без query с только фильтром Витрина имеют название "Избранные проекты" с соответствующим описанием.
  А проекты без query с только фитром Резюме имеют название "Резюме" и тоже какое-нибудь цепляющее поисковое описание.
  И еще если из фильтров только проекты, то это по сути страница "Проекты", а если события, то "События".
  А если вообще нет никаких фильтров, то "Проекты и События"
  Добавить все эти варианты и в sitemap тоже как отдельные ссылки.
  Получается у нас одна страница в зависимости от фильтров может выполнять роль полноценных страниц сайта: Проекты и События, Проекты, События, Избранные проекты, Резюме!
  И даже можно в этих частных случаях убрать слово "Поиск" из заголовка вкладки и SEO.
  Все с соответствующими SEO описаниями согласно семантике этих сущностей.
  Удобно!
  Возможно, если в этом есть смысл, можно сделать для этих страниц особые адреса перенаправления "/cv/ /showcase/ /projects/ /events/".
  Подумай, как это аккуратно реализовать без проблем с технической стороны, но чтобы может и поисковики это видели...

- В боковой панели для секций файлов и ссылок делать кнопку-текст акцентную "Показать все X", если элементов в списке больше 7

- В editor-js редакторе почему-то не получается в абзацах поставить курсор прямо перед первой буквой в абзаце. Курсор "текстовый" превращается в обычный курсор, но еще не pointer для стоящей рядом кнопки настроек блока.
  Что-то не так. Должна быть легкая возможность установить курсор на начало абзаца. Покопайся, найди первопричину и аккуратно устрани ее желательно без грязных хаков.

- Добавить в editor-js вариант инлайнового текста в перечеркнутом формате как третий вариант после жирного и курсива.

- Добавить в editor-js вариант инлайнового элемента "подсказка", который позволяет прикрепить к выделенному тексту какой-то пояснительный текст (через InputTextArea) без возможностей форматирования, просто текст.
  Визуально же это выглядит как dotted подчеркивание под текстом. Опять же, делай попап в редакторе аккуратно, а то с ними много проблем и чтобы можно было так же как и с ссылками поправить подсказку.
  Учти, что InputTextArea может увеличиваться в высоте в зависимости от введенного текста, это тоже надо учитывать наверное в позиционировании попапа? Сделай аккуратно и красиво.

- Проверить, что в editor-js блок ссылка на внешние сайты при открытии самого редактора не выполняет самостоятельно обновление информации из внешних источников, а берет уже сохраненную информацию из общего механизма данных о внешних ссылках.
  Обновление информации должно происходить только в случае, если эту блок-ссылку удалять, а потом ту же самую ссылку вставят еще раз.
  А то слишком много запросов может получаться при открытии редатора, в котором много блок ссылок! Неэффективно и может гразить лимитными ограничениями!

- В публичной части списки из editor-js контента выводи без вот этого идиотского отступа слева, а наравне с другим текстом. И в целом привести внешний вид списков в editor-js к тому, как они выглядят в публичной части (сейчас у них разные дизайны)

- Почему-то длиные безпробельные строчки текстовые что в абзацах, что в списках в публичной части не разрываются автоматом с переносом и выходят за границы элемента.
  Исправь этот ужас - любой контент в публичной части от editor-js вообще не должен никогда вот так вот рвать разметку и всегда должен переноситься и сохраняться в пределах выделенной ему ширины.

- На главной публичной страницы плашки-кнопки закрепленных страниц при hocus наведении должены брать акцентные цвета иконки, а не сайта для цвета текста, границы и фона.

- Попап с настройками цветов, стиля шрифта и прочего в публичной части надо сделать более компактным и с меньшим размером текста, при этом аккуратно, чтобы не задеть если он переисопльзуется на странице установки или в админке "Настройки сайта".
  Отдельно проверить, чтобы в мобильном формате компактно смотрелось.

- Добавить в попап настроек публичный в самом низу информацию, что сайт работает на движке ИКОНКА_THEI Thei ВЕРСИЯ и эта же строчка это ссылка на GitHub страницу.
  Должно выглядеть красиво, коротко и лаконично.
  А еще в head теге метаданные правильные надо дать, что эти все страницы созданы генератором Thei посмотреть изучить как правильно такая мета разметка делается.

- Убрать семантическое расхождение между Витринным и проектом, как это везде называется и "Лучшими проектами" на главной.
  Теперь просто это везде "Витринные проекты". На странице редатирования страницы тоже как вопрос форомить "Витринный проект?" и более коротко описание "Показывать на главной странице и сверху поиска."

- В модалках при работе с файлами подправить фразы. При открытии модалки файла или ассета или медиа или любого другого там есть две кнопки: Заменить и Удалить.
  Так вот это никакая не "Заменить" должна быть, а "Изменить", потому что при клике на нее можно создать как вариант уже использующегося файла, так и уже действительно "Заменить".
  Уже в следующей модалке не "Заменить новым файлом", а просто "Другой файл" потому что он не обязательно новый будет, ведь мы можем выбрать и переиспользовать что-то.

- Еще разок разобраться хорошенько в модалке изменения/вариантов файла.
  Во-первых, почему-то например при изменении файла первой выводится группа настроек "Основа обработки".
  В чем ее смысл? Если оригинал уже может быть даже удален, а в сохраненных вариантах уже измененный только есть?
  Причем в этой "Основе обработке" по сути дублируются данные этого "Настроенного 1".
  Короче разобраться, в каких случаях "Основа обработки" вообще имеет смысл и показывать только тогда, а может и никогда, если не имеет.
  Вообще первая группа должна предназначаться только для ситуации загрузки нового файла и она должна быть именно про оригинальный файл, если он есть.

  Во-вторых, что это за "Заполнить", "Увеличение разрешено" и прочие атрибуты? Они вообще хоть где-то учитываются?
  Во время создания варианта я не вижу, чтобы их как-то можно было настроить.
  Если эти бесполезные атрибуты ненстраиваемые не нужны, может их вообще убрать и из кодовой базы и из UI?
  Подумай хорошенько.
  "Используется сейчас" должно быть отдельной строчкой после всех этих данных.
  Короче, визуально все эти боксы вариантов тоже приведи в порядок к единообразному виду красивому.
  Нам же важно не запутать админа какой-то кучей невнятных данных непонятно откуда и непонятно зачем (добавить может title-popup подсказки еще)

  По сути все эти атрибуты это бесполезный и перегружающий информационный мусор ("настроенный", "оригинал", "заполнить", "увеличено" и т.д.) мешающий восприятию и усложняющий логику кода.
  Через неделю после загрузки файла мне уже будет глубоко пофигу этот файл оригинал, или измененный, через что он там прошел.
  Вот эти все плашки информационные они относятся к процессу трансформации файла, который никому уже не интересен.

  Поэтому я думаю надо в целом провести какое-то переосмысление дизайна этой части, чтобы выводить только актуальные данные файла (не то что с ним "когда-то произошло", а то, чем он является прямо сейчас и какими атрибутами обладает (вес, размеры, наличие аудио и т.д.)). Чисто фактаж. Подумай, как это наиоблее логично и гармонично сделать.

- Расширить систему временных ссылок с доступом и на "страницы" тоже.

- Не давать вводить однозначно недопустимые символы в slug страниц и "читаемую часть URL" проектов, ссылок, тегов и везде где это используется.
  Сделать это переиспользуемой системой нормализации. То есть можно вводить буквы английские и любых других алфавитов, числа, дефисы и любые другие однозначно разрешенные недвусмысленные символы, но не допускать символов, которые точно запрещены или которые могут сломать синтаксис ссылки.

- Если нет звука у видео, то в кастомном UI баре управлений им в модалке вообще не показывать элементов управления звуком, вообще ничего.
  Но и чтобы бар длительности видео не упирался прямо в границу, а имел нормальный отступ!

- Возможность любой блок через панельку со свойствами пометить как "Спойлер". Тогда в редакторе справа от этого блока показывается иконка глаза перечеркнутого в слегкца ацентном цвете с title popup подсказкой, что это спойлер.
  Очень аккуратно реализовывай эту правую иконку, чтобы не сломать ширину других блоков, они все всегда должны иметь одну ширину, что блоки-спойлеры, что обычные!
  Короче подумай, как это сделать наиболее аккуратно, без грязных хаков и

Все изменения проверить в браузере, чтобы ничего не вылазило за границы, не ломалось визуально и выглядело лаконично и понятно.

---

Третий пак правок для версии v0.0.2 (с отдельным планом).
Если потребуется менять форматы и вид URL спокойно меняй и не обеспечивай обратную совместимость старых ссылок.
Если нужно добавлять новые тестовые данные, добавляй их разнообразно в любом количестве и оставь в playground проверки, чтобя мог сам отдельно проверить.
Если нужно входить и выходить из админ панели, то используй имеющийся механизм fallback password простенький, в playground ничего конфиденциальноо нет.

- Улучшение разметки Хронологий (жизни или проекта) (единая переиспользуемая компонентая система):
  - Не отображать собственный фон этой липкой шапки пока она не приклеится (а то иначе ее фон перегораживает верхнюю эллипсоидную подсветку страниц)
  - Когда эта шапка приклеивается, то тень под первой основной шапкой липкой исчезает и та же самая тень появляется уже под липкой второй липкой шапкой
  - Когда шапка приклеена, то слева показывать иконку типа хронологии (сердечко если жизнь, иконка проекта, если проект), дату тоже влево сместить, убирать подписи "Дата Месяц Год" и просто единым боксом с вертикальными разделителями выводить 28 | 04 | 2005 и это теперь все единая ссылка по клику на весь бокс, а не три отдельные ссылки
  - в правом углу шапки кнопка-фильтр по нажатию на которую открывается попап с настройками что показывать в хронологии (можно отключить и включать): события, этапы, статусы, автарки, записи дневника и т.д. (учитывать конекст хронологии, в хронологии проекта статусов и аватарок быть не может конечно же и т.д.) Если фильтры отличаются от "Показывать все", то рядом с иконкой фильтра кружок небольшой акцентный справа сверху индикатор, что фильтры кастомные.
  - В самой ленте с правой стороны для каждого сегмента ленты выводить дату (число + текстом название месяца) крупным блеклым жирным шрифтом. Если это первый сегмент ленты в очередном году, то очень крупным и жирным шрифтом над датой и месяцем сверху выводить год полной цифрой
    И год и даты эти являются ссылками, при наведении слегка акцентный цвет получают и при клике копируют в буфер обмена ссылку на эту секцию ленты, а так же делают активным сегмент ленты жизни с обновлением UI и URL
  - Сегменты вертикальной прямой "стрелы хронологии" слева тоже являются ссылками (не отдельно по произошедшим событиям, а целиком на всю дату, то есть на весь вертикальный сегмент).
    Поэтому на них отображается cursor pointer, клик по ним опять же копирует ссылку на эту дату в ленте, а так же этот сегмент становится "активным", подсвечивается и в липкой шапке тоже устанавливается эта дата и URL страницы тоже обновляется вместе с заголовком вкладки.
  - По части URL теперь ссылаться можно только на датированные цельные сегменты в формате параметра запроса /life/?d=гггг-мм-дд|projects/.../feed(life?,???)/?d=гггг-мм-дд
    Такой формат позволяет так же внедрить все фильтры тоже прямо в URL, чтобы им можно было делиться.
  - Убрать поддержку старого типа вложенных URL /гггг/мм/дд/, /гггг/ и /гггг/мм/ и не делать обратной совместимости.

- Расширить систему сохраняемых статусов и на проекты тоже.
  То есть теперь и каждый проект в отдельности может иметь свой статус с их историей.
  Эти статусы можно редактировать на админ странице редактирования проекта с точно таким же UI, как и на админ странице "Обо мне".

  Везде где используется (включая хронологические ленты) иконку общего статуса и статуса проектов измени с "Цитата" на "Пульс" (медицинское обозначение пульса/сердечного ритма).

  Статус проекта отображать во вкладке "Обзор" в самом верху отдельным боксом во всю ширину примерно в стилистике как на главной странице с "Показать все" если статусов несколько и живой подгрузкой элементов списка.
  В этом плане можешь по максимуму переиспользовать.

  В Хронологических лентах (жизни и проекта) тоже выводи новые статусы проектов. В целом создай единый компонент для отображения статуса в таких лентах с допилами в завимиости от того это "Новый статус жизни" или "Новый статус проекта".
  В случае проекта где-то аккуратно надо небольшую иконку проекта с его названием и ссылкой на страницу проекта выводить.

- Страницу проекта изменить.
  Hero шапка без изменений, а ниже как его составная самая нижная часть полоса узкая с глобальным разделением на две вкладки "Обзор" и "Хронология" (feed по английски или есть более удачное короткое слово?) (обе вкладки с иконками своими) (у вкладки Хронология справа циферка в скругленном боксе, обозначающая количестве связанных элементов ленты с этим проектом)

  В режиме "Обзор" остается старая двухколоночная разметка, статус описание этапы заметки и т.д., но убираем "встроенную" хронологию по этапам, вместо нее делаем секцию с тремя последними хронологическими карточками (статусами, записями дневника, этапами, связанными событиями) друг по другом в список, а кнопка "Смотреть все" (или как теперь она называться будет?) переключает режим на "Хронологию". Еще убираем "Связанные события", потому что аналогичный результат можно получить в Хронологии с нужными фильтрами, отдельную страницу нет смысла держать.
  В целом это почти как блок "Последнее из жизни", только "Последнее из хронологии" проекта.

  На вкладке "Хронология" появляется по сути все то же самое что и на странице "Жизнь" (это теперь единая переиспользуемся система): одна колонка во всю ширину, вторая липкая шапка с датами и фильтрами, слева "стрела событий" с "кружочками точками интереса иконками" и так далее, карточки, крупные даты справа и т.д. Показываются только элементы ленты, которые так или иначе имеют отношения к проекту: статусы проекта, записи дневника относящиеся к проекту, события связаные с проектом, этапы проекта, даты создания разделов проекта ну и все такое. Короче это подмножество элементов ленты "Жизнь", но со всем, что имеет отношение к этому проекты.
  Естественно под это нужно заточить и URL систему, чтобы у хронологии конкретного проекта был четкий URL так же с отдельными URL под каждый сегмент ленты, в которые что-то да происходило.

- "Хронология" в боковой панели надо переименовать как-то иначе в что-то вроде "Основные даты" или как-то так. И там выводить так же дату первого/последнего статуса, первой и последней связанной записи дневника и т.д. естественно с склеиванием в однин элемент списка, если оно одновременно и первое и последнее. Причем для статусов, этапов, разделов и записей дневника эти элементы списка "Основных дат" можно сделать ссылками.

- Атрибут блоков: Спойлер

- Блок настроек "Связанные проекты" переименовать в "Связанные сущности" и позволять искать и прикреплять не только проекты, но и события вперемешку вместе с проектами и тоже с тремя типами связей.
  Так же переиспользовать этот блок и добавить его и к событиям и к записям дневника.
  Секцию "Связанные проекты" в боковых панелях на публичных страницах тоже переименовать в "Связанные сущности" и разделить на три подсписка, как у файлов и ссылок: безымянный первый список из связей типо "связанны", далее обязательно с подзаголовком и иконкой "Зависит от" и "Влияет на" и иконки слева от этих названий. У элементов списка в правом нижнем углу вместо иконки типа связи ставить иконку типа сущности (проект или событие).
  Перерисовать иконки всех трех типов связи в более понятные, читаемые и без "проектовой" рамки как сейчас, потому что уже не только проекты могут быть в связях.

- Новый основной тип сущности движка - "Запись дневника" даже без возможности указать заголовоки и суть, но с обязательной датой (одной датой) и обязательным ContentEditor полем "Содержимое записи". Типа доступа два "Публичый и Приватный (но с возможностью создать ссылку для просмотра)" В хронологиях уникальная иконка "облачка мысли". В поиске не участвует, в AdminBar сверху добавь быструю кнопку с счетчиком. Запись дневника является третим "основным типом" сущностей в Thei и самым слабым.

  Карточка дневника в хронологиях тоже попроще, все еще с MediaEdge, но в качестве текста курсивом выводить первые X символов (максимум где-то 4 строчки текста на карточку) текстовый контента из записи дневниковой с обрезкой по ellipsis (если есть)

  URL записей дневника фиксированного формата, без случайного publicId и читаемую часть нельзя настроить. Это "/diary/гггг-мм-дд/" и все - коротко и лаконично.

  В боковых панелях проектов и событий новая секция "Записи дневнка", в которых отображаются первые 5 связанных записей дневника в формате даты если нет названия и названия записи + бледная дата если есть название. Далее "Показать все", которая от... Нет, что-то не то. Записей дневника может быть много связанных, десятки. Для проектов не проблем -- они отображаются в его отдельной хронологии. КАК ОТОБРАЖАТЬ связанные записи дневника в событиях? Отдельным полем снизу? В боковую панель десятки записей влезут? Подумать.

  В sitemap.xml с отдельным SEO заголовком, описанием и заголовком вкладки "Дневник" занести вариант страницы life с фильтром "только записи дневника". И отдельное перенаправление с "/diary/"?

- У всех карточек улучшить систему отображения связанных сущностей. Слева выводить иконку сотрящих друг на друга стрелок (как у блока с настройками связанных сущностей в админке), а справа от него если только одна сущность связана, то иконку квадратную со скошенными углами и подложкой + полное название сущности, а при наведении ее суть. Если же более одной сущности привязано, то просто ряд таких иконок уже без названий и при наведении показывать их названия. И то и другое это ссылки, которые при нажатии открывают соответствующие страницы сущностей. Не добавлять общий фон общего бокса вокруг и иконки и названия, это лишнее. В хронологии проектов из связанных событий и записей дневника не удалять связанный проект, который прямо сейчас и открыт, а то теряется связность.
