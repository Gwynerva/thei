export enum ProjectEventAccessLevel {
  Public = 'public',
  LinkOnly = 'link-only',
  Private = 'private',
}

export enum AssetAccessLevel {
  Public = 'public',
  Private = 'private',
}

export enum SiteAccessLevel {
  Public = 'public',
  Private = 'private',
}
